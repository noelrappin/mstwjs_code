<!DOCTYPE html>
<html>
<head>
  <title>Trips: <%= controller.action_name %></title>
  <%= stylesheet_link_tag    "application", :debug => true %>
  <%= javascript_include_tag :application, :debug => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <h1 class="span-10">
    <%= link_to "Time Travel Adventures", root_path%>

  </h1>

  <div class="span-14 last">
    <% if current_user %>
      <div>Logged in as <%= current_user.first_name %> <%= current_user.last_name %></div>
      <div><%= link_to "Log Out", destroy_user_session_path %></div>
    <% else %>
    <div><%= link_to "Log In", new_user_session_path, :id => "log_in" %></div>
    <div><%= link_to "Sign Up", new_user_path %></div>
    <% end %>
  </div>
  <br clear="all" />
  
  <% if notice.present? %>
    <p class="notice"><%= notice %></p>
  <% end %>
  <p class="alert"><%= alert %></p>
  
  <div class="span-4">
    Things To Do:
    <br />
    <% if current_user %>
      <%= link_to "My Profile", user_path(:id => current_user) %>
      <br/>
      <%= link_to "Edit My Profile", edit_user_path(:id => current_user) %>
      <br />
      <%= link_to "Edit My Preferences", user_preferences_path %>
      <br />
      <%= link_to "Make Friends", users_path %>
    <% end %>
    <br />
    <%= link_to "More about time travel", "http://en.wikipedia.org/wiki/Time_travel" %>
  </div>

  <div class="span-20 last">
    <%= yield %>
  </div>

</body>
</html>
